{
  "name": "project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "vite build --logLevel=info --outDir=dist/server --emptyOutDir=true --ssr=app.server.ts",
    "bundle": "./node_modules/.bin/esbuild --bundle --outfile=dist/server/app.server.cjs --format=cjs --allow-overwrite dist/server/app.server.js"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "6.2.1",
    "esbuild": "^0.27.3",
    "svelte": "5.53.2",
    "vite": "7.3.1"
  }
}
